<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.terna.projectone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eqplot_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/eqplot_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Graph3D" android:screenOrientation="fullSensor"/>
        <activity android:name=".NestedLayout2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".History" />
        <activity android:name=".Graph" android:screenOrientation="fullSensor"/>
        <activity android:name=".EquationMaker" />
        <activity android:name=".Sample" />
    </application>

</manifest>